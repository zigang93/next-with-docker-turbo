# Nextjs with docker example and turbo
# docker command 

build with full log

```zsh
PROGRESS_NO_TRUNC=1 docker build --progress plain -t next-js .
```
remove docker image

```zsh
docker rmi next-js
```
